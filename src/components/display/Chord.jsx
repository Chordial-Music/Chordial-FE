import React from 'react';
import ChordList from 'ChordList';

export default function Chord() {
  // This renders out an individual chord node from the chord array
  // We get this data from ChordList.jsx
  // When the user clicks on one of these chords, it should create a new ChordList
  return (
    <div>

    </div>
  )
}
